/**
 * Enumeration of all possible move patterns a ghost can have.
 * The five values are hunt, escape, disperse, getout and timeout.
 * @author Mira
 */
public enum GhostMovePattern {
	HUNT,
	ESCAPE,
	DISPERSE,
	GETOUT,
	TIMEOUT
}
